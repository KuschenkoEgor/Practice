package main

import "fmt"

/* Найти наибольший общий префикс из массива строк*/

func longestCommonPrefix(str []string) /*string*/ {
	//str := []string {"flower","flow","flight"}
	//fmt.Println(str[0])
}

func main() {
	str := []string {"flower","flow","flight"}
	fmt.Println(len(str))
	i :=0
	for i < len(str) {
		k = []rune(str[i])
		i++
	}
	for k:=0; k<15;k++ {
		if
	}
}